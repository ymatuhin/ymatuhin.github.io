{% if url %}
<figure>
		<iframe
			width="{{ width }}"
			height="{{ height }}"
			style="aspect-ratio: {{ width }} / {{ height }}"
			sandbox="allow-scripts allow-same-origin"
			src="{{ url }}"></iframe>{% if caption %}<figcaption>{{ caption }}</figcaption>{% endif %}</figure>
{% endif %}
